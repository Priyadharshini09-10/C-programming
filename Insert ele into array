#include <stdio.h>

int main()
{
    int i,n=4,pos=2,key=3;
    int found=0;
    int arr[10]={1,2,4,5};
    for(i=n;i>pos;i--){
        arr[i]=arr[i-1];
    }
     arr[pos]=key;
     n++;
   
for(i=0;i<n;i++){
    printf("%d ",arr[i]);
}
    return 0;
}
