#include <stdio.h>
#include<string.h>
int main()
{
    char str[100];
    fgets(str,sizeof(str),stdin);
    int len=strlen(str);
    if(str[len-1]=='\n'){
        str[len-1]='\0';
    }
    len=strlen(str);
    int curwrd;
    int curlen=0;
    int maxlen=0;
    int wrdsrt;
    int start=0;
    for(int i=0;i<len;i++){
        if(str[i]!=' '){
            if(curlen==0){
                wrdsrt=i;
            }
            curlen++;
        }
        else{
            if(curlen>maxlen){
                maxlen=curlen;
                start=wrdsrt;
            }
        
        curlen=0;
        }
    }
    if(curlen>maxlen){
    maxlen=curlen;
    start=wrdsrt;
}
for(int i=start;i<start+maxlen;i++){
    printf("%c",str[i]);
}
    printf("\n%d",maxlen);

    return 0;
}
