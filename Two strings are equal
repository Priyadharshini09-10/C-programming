#include <stdio.h>
#include<string.h>
int main()
{
   char str1[100];
   char str2[100];
   fgets(str1,sizeof(str1),stdin);
   fgets(str2,sizeof(str2),stdin);
   int len1=strlen(str1);
   int len2=strlen(str2);
   if(str1[len1-1]=='\n'){
       str1[len1-1]='\0';
   }
   if(str2[len2-1]=='\n'){
       str2[len2-1]='\0';
   }
   len1 = strlen(str1);
len2 = strlen(str2);
   int i,j;
   if(len1!=len2){
       printf("Not equal");
       return 0;
   }
   for(i=0;i<len1;i++){
       if(str1[i]!=str2[i]){
           printf("Not equal");
           return 0;
       }
   }
   printf("Equal");

    return 0;
}
