#include <stdio.h>
int main()
{
   int i,n=5;
   int arr[]={10,20,30,40,50};
   int temp=arr[n-1];
   for(i=n-1;i>0;i--){
       arr[i]=arr[i-1];
   }
   arr[0]=temp;
for(i=0;i<n;i++){
    printf("%d ",arr[i]);
}
    return 0;
}
