#include <stdio.h>

int main()
{
    int n=4,i,j;
    int arr[]={1,2,3,4};
    int tar=6;
    for(i=0;i<n;i++){
        for(j=i+1;j<n;j++){
            if(tar==arr[i]+arr[j]){
                printf("%d %d",arr[i],arr[j]);
                break;
            }
        }
    }

    return 0;
}
